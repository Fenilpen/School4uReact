function Ifcomponent(){

const option = "a"


if(option === "a"){
  return(
  <div>
    <h1>component A</h1>
  </div>

) }

if(option === "b"){
  return(
  <div>
    <h1>component B</h1>
  </div>

) }


if(option === "c"){
  return(
  <div>
    <h1>component C</h1>
  </div>

) }


if(option === "d"){
  return(
  <div>
    <h1>component D</h1>
  </div>

) }

}

export default Ifcomponent
